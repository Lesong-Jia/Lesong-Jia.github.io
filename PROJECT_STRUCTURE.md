# 项目结构说明

## 📁 目录结构

```
src/
├── assets/images/          # 图片资源
│   ├── Cover_V3.png       # 首页封面图
│   ├── Cover.png          # 备用封面图
│   └── Self_Photo.jpg     # 个人照片
│
├── components/            # 组件（按功能分类）
│   ├── Collaborators/     # 合作者相关
│   ├── Footer/            # 页脚
│   ├── Hero/              # 首页 Hero 区块
│   ├── Nav/               # 导航栏
│   ├── News/              # 新闻区块
│   ├── Publications/      # 研究项目区块
│   ├── SelfIntro/         # 自我介绍
│   ├── Seo/               # SEO 优化
│   └── SiteLogo/          # Logo
│
├── config/                # 配置文件
│   ├── navData.json.ts    # 导航菜单配置
│   └── siteData.json.ts   # 网站基本信息
│
├── data/                  # 内容数据（Markdown + 图片）
│   ├── collaborators/     # 合作者数据
│   ├── news/              # 新闻数据
│   └── publications/      # 发表数据
│
├── layouts/               # 布局文件
│   ├── BaseHead.astro     # HTML head
│   └── BaseLayout.astro   # 基础布局
│
├── pages/                 # 页面路由
│   ├── index.astro        # 首页
│   ├── 404.astro          # 404 页面
│   ├── collaborators/     # 合作者页面
│   ├── news/              # 新闻页面
│   └── publications/      # 研究项目页面
│
└── styles/                # 样式文件
    ├── global.css         # 全局样式
    ├── tailwind-theme.css # Tailwind 主题
    └── ...
```

## 📝 内容管理

### 添加新闻 (`src/data/news/`)

每个新闻一个文件夹，命名格式：`news-N/`

**必需文件：**
- `index.md` - 新闻内容

**可选文件：**
- `cover.jpg` - 封面图

**index.md 格式：**
```yaml
---
title: "新闻标题"
description: "新闻摘要"
heroImage: ./cover.jpg  # 可选
author: "作者"          # 可选
date: "2025-04-01"
order: 1
link: "https://..."     # 可选，外部链接
---

新闻正文...
```

### 添加发表 (`src/data/publications/`)

每个发表一个文件夹，命名格式：`pub-N/`

**必需文件：**
- `index.md` - 发表信息

**可选文件：**
- `thumb.jpg` - 缩略图

**index.md 格式：**
```yaml
---
title: "论文标题"
venue: "会议/期刊"       # 可选
authors: "作者1, 作者2, Lesong Jia"
year: 2025
doi: "10.xxx/xxx"       # 可选
thumbnail: ./thumb.jpg   # 可选
direction: "多模态交互"   # 研究方向
order: 1
---
```

### 添加合作者 (`src/data/collaborators/`)

每个合作者一个文件夹，命名格式：`collab-N/`

**必需文件：**
- `index.md` - 合作者信息

**可选文件：**
- `photo.jpg` - 照片

**index.md 格式：**
```yaml
---
name: "姓名"
title: "职位"
image: ./photo.jpg       # 可选
period: "2022-2025"      # 时间段
order: 1
showOnHomepage: true     # 是否在首页显示
homepageOrder: 1         # 首页显示顺序
---

合作者介绍...
```

## 🎨 样式定制

- **主题颜色**：`src/styles/tailwind-theme.css`
- **全局样式**：`src/styles/global.css`
- **字体配置**：`src/styles/fonts.css`

## ⚙️ 配置修改

- **导航菜单**：`src/config/navData.json.ts`
- **网站信息**：`src/config/siteData.json.ts`
- **内容结构**：`src/content.config.ts`

## 🚀 开发流程

1. **添加内容**：在对应的 `src/data/` 文件夹下创建新文件夹和文件
2. **修改样式**：编辑 `src/styles/` 下的 CSS 文件
3. **调整布局**：修改 `src/components/` 下的组件文件
4. **测试**：运行 `npm run dev` 查看效果
5. **构建**：运行 `npm run build` 生成生产版本
